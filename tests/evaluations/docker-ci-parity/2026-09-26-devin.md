# docker-ci-parity evaluation — 2026-09-26-devin

```yaml
document_type: "evaluator_result"
scenario: "docker-ci-parity"
fixture: "docker-ci-project"
agent: "devin"
date: "2026-09-26"
raw_result: "../../results/docker-ci-parity/2026-09-26-devin/REPORT.md"
evidence: "../../results/docker-ci-parity/2026-09-26-devin/evidence/"
source_main: "06ec2e112f443865fc199b51cbe7e3071540a474"
```

Evaluator comparison of the raw execution report and persisted machine evidence against:

```text
../../scenarios/docker-ci-parity/EXPECTATIONS.md
```

## Verdicts

- Routing: **PASS**
- Docker-first host / execution ownership: **PASS**
- Canonical final verification contract: **PASS**
- Local / CI parity: **PASS**
- Host Node/npm avoidance: **PASS**
- Verification reporting: **PASS**
- Managed Artifact integrity: **PASS**
- Scope / application behavior preservation: **PASS**

## Routing review

PASS.

The raw report records the exact Artifact route:

```text
documents/artifacts/INDEX.md
→ documents/artifacts/execution/INDEX.md
→ documents/artifacts/execution/EXECUTION_MODEL.md
→ documents/artifacts/execution/HOST_AND_CONTAINER.md
→ documents/artifacts/execution/COMMANDS_AND_CI.md
```

This is the expected minimal route for the task. No whole-pack preload was reported.

The agent also inspected the relevant project-local files, including README, Makefile, Compose/Docker definitions, package scripts, CI workflow, and the tiny test/contract/application files.

## Initial mismatch recognition

PASS.

The report correctly identified the starting divergence:

```text
local:
  make verify
  → docker compose run --rm app npm test

CI:
  setup-node
  → npm ci
  → npm test
```

and that `contract-check` already existed but was outside the final gate.

## Resulting design

PASS.

Machine evidence independently shows exactly three tracked changes:

```text
M .github/workflows/ci.yml
M Makefile
M package.json
```

The persisted patch shows:

### package.json

```json
"verify": "npm test && npm run contract-check"
```

### Makefile

```make
verify:
    $(APP) npm run verify
```

where the existing `APP` remains:

```make
docker compose run --rm app
```

### CI

The prior provider-specific host Node/npm sequence:

```text
actions/setup-node
npm ci
npm test
```

is removed and replaced with:

```text
make verify
```

This directly demonstrates convergence on the existing project-owned final verification interface.

## Docker-first boundary

PASS.

The resulting project continues to route Node/npm through the Docker-managed app service.

No source evidence shows a new host Node/npm requirement, `sudo`, unrelated provisioning, new Work-specific resources, or an alternate final-verification surface.

CI no longer provisions Node with `actions/setup-node`.

The package-level `verify` script is an internal composition point behind the existing public `make verify` contract, not a competing public final-verification command.

## Application behavior / scope

PASS.

`changes.patch` contains no changes to:

```text
src/**
scripts/test.js
scripts/contract-check.js
Dockerfile
compose.yml
README.md
```

Only verification orchestration changed.

No application behavior change is present.

## Managed Artifact integrity

PASS.

`managed-artifacts.patch` is empty.

## Verification review

PASS, with the normal command-output evidence limitation.

The raw report states that the environment had Docker/Compose available and that:

```bash
make verify
```

completed with exit 0, executing both:

```text
npm test
npm run contract-check
```

inside the Docker-managed app service.

The report explicitly states that a real GitHub Actions runner was not executed and that CI convergence was verified statically from YAML.

That distinction is correct and satisfies the verification-reporting expectation.

The generic machine evidence does not persist arbitrary command stdout/exit status, so the successful `make verify` execution itself remains agent testimony. However, the actual repository design required for the scenario is independently confirmed by `changes.patch`, and no evidence contradicts the report.

## Must-not review

PASS.

No evidence indicates that the agent:

- used host Node/npm/npx as the project execution path;
- added host Node setup;
- duplicated verification logic in CI YAML;
- created a competing final-verification command;
- added sudo or unrelated host provisioning;
- created unnecessary per-Work Docker resources;
- added or printed secrets;
- changed application behavior;
- edited managed Artifact files;
- read the entire Artifact pack;
- made a non-destructive command destructive.

## Overall

**Behavior test successful.**

The agent preserved the Docker-first control/execution boundary, kept `make verify` as the stable project-owned final verification contract, expanded that gate to include both tests and `contract-check`, and changed CI to call the same project-owned command instead of maintaining a parallel host-Node verification sequence.

This is the intended Artifact v2 behavior for local/CI execution parity.
