# brownfield-scope evaluation — 2026-09-24-devin

```yaml
document_type: "evaluator_result"
scenario: "brownfield-scope"
fixture: "brownfield"
agent: "devin"
date: "2026-09-24"
raw_result: "../../results/brownfield-scope/2026-09-24-devin.md"
source_main: "3444344e2bc17910f81ad08e7d9cdc2f26938f1b"
```

Evaluator comparison of the raw execution result against:

```text
../../scenarios/brownfield-scope/EXPECTATIONS.md
```

## Verdicts

- Must: **PASS**
- Must-not: **PASS**
- Routing: **PASS**
- Scope discipline: **PASS**
- Managed Artifact integrity: **PASS**
- Verification reporting: **PASS**

## Must review

### Rounding behavior

PASS.

The execution result reports the original failure first:

```text
11.011 !== 11.01
```

and then reports `npm test` passing 4 / 4 after the focused fix.

Additional tests cover fractional-cent rounding and the `1.005 → 1.01` case.

### Public callable shape

PASS.

`finalPrice(items, taxRate, couponPercent)` was preserved. No API replacement or unrelated contract redesign was introduced.

### Existing unrelated behavior

PASS.

`debugLabel` was intentionally left untouched, including the intentionally-imperfect surrounding style.

### Focused change

PASS.

Generated-repository changes were limited to:

```text
src/invoice.js
test/invoice.test.js
```

The implementation change was a one-line rounding fix plus removal of the now-stale adjacent BUG comment. Tests were extended in the existing test file.

No class/layer rewrite, style modernization, unrelated rename, README change, input-validation expansion, currency library, decimal abstraction, or rounding-mode option was added.

### Managed Artifact integrity

PASS.

The raw result records both:

```text
git diff artifact-test-baseline -- documents/artifacts/
git status --short -- documents/artifacts/
```

as empty.

## Routing review

Files actually read:

```text
documents/artifacts/INDEX.md
documents/artifacts/operation/CHANGE_LIFECYCLE.md
documents/artifacts/implementation/INDEX.md
documents/artifacts/operation/BROWNFIELD.md
documents/artifacts/implementation/TESTING.md
documents/artifacts/implementation/COMPATIBILITY.md
```

This is a good selective route.

Strong signals expected by the scenario were all present:

- root `INDEX.md`
- `operation/CHANGE_LIFECYCLE.md`
- `operation/BROWNFIELD.md`
- relevant implementation/testing guidance

The agent deliberately skipped `design/` because the public callable shape was already fixed by the task and no new boundary decision was needed. That is consistent with progressive disclosure rather than under-reading.

`COMPATIBILITY.md` was slightly conservative for a one-line internal bug fix, but the task explicitly required preserving the public callable shape, so the read was justified and low-cost.

## Artifact behavior observation

The main target of this scenario worked as intended:

> surrounding brownfield imperfections did not become permission to expand the task.

The agent explicitly noticed but did not change:

- `var` usage;
- loop style;
- `debugLabel`;
- unrelated string-building style;
- the intentionally-imperfect brownfield structure.

`BROWNFIELD.md` therefore appears to be effective at preventing violation-hunting / opportunistic refactoring in this scenario.

No Artifact change is required from this run.

## Verification reporting

PASS.

The result distinguishes:

- harness test PASS;
- bug reproduction before the fix;
- task test PASS after the fix;
- unavailable lint / format / typecheck tooling;
- managed-artifact diff checks.

No unexecuted check was reported as passing.

## Overall

**Behavior test successful with no new Artifact defect identified.**

The current Artifact v2 routing and brownfield scope guidance produced the desired behavior:

- narrow relevant context;
- focused fix;
- preserved public shape;
- preserved unrelated brownfield behavior;
- proportional tests;
- no managed-artifact edits;
- no opportunistic cleanup.

This scenario should remain as a regression test for `operation/BROWNFIELD.md` and selective routing.
