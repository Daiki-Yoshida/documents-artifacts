# documentation-routing evaluation — 2026-09-25-devin

```yaml
document_type: "evaluator_result"
scenario: "documentation-routing"
fixture: "documented-project"
agent: "devin"
date: "2026-09-25"
raw_result: "../../results/documentation-routing/2026-09-25-devin.md"
source_main: "2b62b70373c33e56e1f315e32021282e49b636f6"
```

Evaluator comparison of the raw execution result against:

```text
../../scenarios/documentation-routing/EXPECTATIONS.md
```

## Verdicts

- Must: **PASS**
- Must-not: **PASS**
- Routing: **PASS**
- Existing owner reuse: **PASS**
- Entry-point non-duplication: **PASS**
- Managed Artifact integrity: **PASS**
- Verification reporting: **PASS**

## Must review

### Reusable Artifact routing

PASS.

The run started from:

```text
documents/artifacts/INDEX.md
```

then selectively routed through:

```text
documents/artifacts/documentation/INDEX.md
documents/artifacts/documentation/PRINCIPLES_AND_ROUTING.md
documents/artifacts/documentation/WORKFLOW_AND_MAINTENANCE.md
```

This is directly aligned with the strong routing signals in the evaluator expectations.

The agent explicitly skipped `operation/CHANGE_LIFECYCLE.md` because the documentation-specific guidance already covered the necessary workflow. That smaller route is justified by the scenario expectations and does not indicate missing guidance.

### Project-local routing and ownership

PASS.

The agent inspected the project-local entry/routing chain:

```text
AGENTS.md
README.md
documents/INDEX.md
```

and discovered the explicit owner:

```text
HTTP client policy (timeouts, retries, headers)
→ project/HTTP_CLIENT.md
```

It therefore updated the existing authority rather than creating a competing document.

### Documentation change location

PASS.

The raw result records exactly one changed documentation file in the generated repository:

```text
documents/project/HTTP_CLIENT.md
```

The retry policy was added there as a new `## Retries` section.

No new HTTP policy document was created.

### Retry policy fidelity

PASS.

The report records:

```text
GET: at most two retries on 502/503 with exponential backoff
POST: never automatically retried
timeout: existing per-attempt timeout continues to apply
```

This matches the scenario task.

The existing timeout value of **8 seconds per attempt** was preserved rather than rewritten.

### Routing stability and duplicate authority

PASS.

The agent intentionally left:

```text
documents/INDEX.md
README.md
AGENTS.md
```

unchanged because no new owning document was introduced.

The reported grep verification found retry content only in the existing HTTP owner document, avoiding duplicate policy authority.

### Scope discipline

PASS.

No documentation tree restructure, unrelated rewrite, cleanup, or competing owner was reported.

`documents/project/OPERATIONS.md` and other unrelated project documents remained untouched.

### Managed Artifact integrity

PASS.

The raw result records:

```text
git diff artifact-test-baseline -- documents/artifacts/
```

as empty.

No managed Artifact file was changed.

## Verification review

PASS.

The reported verification includes:

- checking the HTTP route in `documents/INDEX.md`;
- searching documentation for duplicate retry policy;
- `git status --short`;
- `git diff --stat artifact-test-baseline`;
- full baseline diff inspection;
- managed Artifact diff.

The final generated-repository Git state reports exactly one modified file:

```text
documents/project/HTTP_CLIENT.md
```

with eight inserted lines.

## Evidence limitation

The generated temporary repository itself is not persisted in this repository.

Accordingly, this evaluation compares the committed raw execution evidence and PR report against `EXPECTATIONS.md`; it is not an independent evaluator rerun of the temporary repository.

The raw report is internally consistent with the PR summary and includes the requested ownership, routing, diff, duplication, and Artifact-integrity checks.

## Artifact behavior observation

This scenario validates the documentation ownership/routing behavior:

```text
durable knowledge request
→ inspect project-local documentation routing
→ discover existing owner
→ update that owner only
→ keep entry points and routing non-duplicative
→ stop
```

The execution agent did not turn reusable documentation guidance into a universal file-layout rewrite. Project-local authority determined where the durable knowledge belonged.

No Artifact change is required from this run.

## Overall

**Behavior test successful.**

The requested retry policy was routed to the existing HTTP client authority, existing timeout semantics were preserved, routing remained discoverable, and no competing authority or unrelated documentation churn was introduced.

No merge-blocking Artifact defect was identified.
