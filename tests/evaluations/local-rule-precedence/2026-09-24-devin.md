# local-rule-precedence evaluation — 2026-09-24-devin

```yaml
document_type: "evaluator_result"
scenario: "local-rule-precedence"
fixture: "structured"
agent: "devin"
date: "2026-09-24"
raw_result: "../../results/local-rule-precedence/2026-09-24-devin.md"
source_main: "7a57d1624df1f5716a10e7f922bf2d81146782a0"
```

Evaluator comparison of the raw execution result against:

```text
../../scenarios/local-rule-precedence/EXPECTATIONS.md
```

## Verdicts

- Must: **PASS**
- Must-not: **PASS**
- Routing: **PASS**
- Local-rule precedence: **PASS**
- Managed Artifact integrity: **PASS**
- Verification reporting: **PASS**

## Must review

### Project-local instructions

PASS.

The agent explicitly read root `AGENTS.md` and applied each relevant specialization:

- retained the functional-module style;
- introduced no classes;
- kept public user IDs as strings;
- kept source under `src/modules/`;
- kept tests under `spec/`;
- introduced no `domain/`, `application/`, or `infrastructure/` layers;
- treated `documents/artifacts/` as read-only managed guidance.

### Public IDs

PASS.

Leading-zero IDs remained strings. The new implementation filters existing stored records by string prefix and does not convert IDs to numbers.

### Existing behavior

PASS.

`findUserById` was preserved and the generated-repository test suite passed 5 / 5.

### File / structure placement

PASS.

Generated-repository changes were limited to:

```text
src/modules/users.js
spec/users.test.js
```

No alternate project structure was introduced.

### Managed Artifact integrity

PASS.

The raw result records empty:

```text
git diff artifact-test-baseline -- documents/artifacts/
git status --short -- documents/artifacts/
```

## Routing review

Files actually read:

```text
documents/artifacts/INDEX.md
documents/artifacts/design/INDEX.md
documents/artifacts/design/CONTRACTS.md
documents/artifacts/operation/CHANGE_LIFECYCLE.md
documents/artifacts/implementation/INDEX.md
documents/artifacts/implementation/COMPATIBILITY.md
documents/artifacts/implementation/TESTING.md
```

The route is selective and justified.

The agent correctly skipped `design/BOUNDARY_HORIZON.md`: the relevant structural boundary was already specialized by the local project rules, so no new horizon decision was needed.

The generic Artifact did not override explicit project-local specialization. This is the central success criterion of the scenario.

## Artifact behavior observation

The primary scenario target worked as intended:

> Artifact v2 behaved as reusable guidance rather than a universal project-shape generator.

The agent explicitly recognized that generic guidance may discuss architectural patterns that this project's `AGENTS.md` intentionally excludes, and followed the local rules instead.

The recently-added `CONTRACTS.md` guard also had a positive effect: the agent deliberately did **not** promote Map insertion order into a caller-visible ordering guarantee and wrote tests that assert membership rather than incidental order.

## Secondary observation — invalid-domain behavior

The agent also added and tested:

```text
non-string prefix -> []
```

This was not required by the scenario or by the local instruction that public user IDs remain strings.

It is understandable as a local judgment to avoid implicit JavaScript coercion, but once a test is added it becomes a stable caller-visible behavior. That creates a smaller version of the over-contracting risk found in the previous `contract-boundary` run.

This does **not** fail the current scenario: local-rule precedence, structure, scope, and requested behavior all pass.

However, it suggests a future contract-guidance question:

> When an input has a declared valid domain, should behavior outside that domain remain unspecified/preconditioned unless a requirement or project failure convention requires a defined response?

Do not change Artifact guidance from this single observation alone without a deliberate follow-up decision or scenario.

## Verification reporting

PASS.

The raw result distinguishes:

- harness PASS;
- task test PASS 5 / 5;
- unavailable lint / format / typecheck tooling;
- managed-artifact diff checks.

No unexecuted check was reported as passing.

## Overall

**Behavior test successful.**

Artifact v2 correctly respected project-local specialization and avoided imposing generic architecture on the fixture.

No merge-blocking Artifact defect was found.

One secondary observation remains for future investigation: avoid accidentally turning behavior outside a declared valid input domain into a public guarantee without a requirement or established project convention.
