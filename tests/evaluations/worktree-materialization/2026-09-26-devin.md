# worktree-materialization evaluation — 2026-09-26-devin

```yaml
document_type: "evaluator_result"
scenario: "worktree-materialization"
fixture: "worktree-project"
agent: "devin"
date: "2026-09-26"
raw_result: "../../results/worktree-materialization/2026-09-26-devin/REPORT.md"
evidence: "../../results/worktree-materialization/2026-09-26-devin/evidence/"
source_main: "39e74574d58e8938e67db63778e5da39363f08f8"
```

Evaluator comparison of the raw execution report and machine-generated evidence against:

```text
../../scenarios/worktree-materialization/EXPECTATIONS.md
```

## Verdicts

- Must: **PASS**
- Must-not: **PASS**
- Routing: **PASS**
- Deterministic identity / branch / base / path resolution: **PASS**
- Real linked-worktree materialization: **PASS with evidence limitation**
- Recursive project-level `.worktrees/**` exclusion: **PASS**
- Primary repository cleanliness: **PASS**
- Managed Artifact integrity: **PASS**
- Scope / stop-before-implementation: **PASS**
- Machine evidence completeness for worktree-specific state: **IMPROVEMENT NEEDED**

## Must review

### Reusable Artifact routing

PASS.

The raw report records the exact Artifact route:

```text
documents/artifacts/INDEX.md
→ documents/artifacts/project/WORKTREES.md
```

No unrelated Artifact leaves were reported as read.

This is the expected minimal route for the scenario.

### Project-local conventions

PASS.

The report records reading:

```text
README.md
.gitignore
.worktrees/PROJECT_COORDINATION.md
src/app.js
```

The important project-local decisions match the fixture:

```text
Work Identity:      feat/audit-log-export
repository selector: main
branch:             feat/audit-log-export
base:               main
path:               .worktrees/feat/audit-log-export/main/
```

The already-confirmed Work Identity was not re-gated.

### Deterministic base / branch handling

PASS.

The reported commands explicitly create the Work branch from the documented base:

```bash
git branch feat/audit-log-export main
```

before worktree materialization.

The report also records both `main` and `feat/audit-log-export` at generated baseline SHA:

```text
3ce458256ab829d7a38bf9ee0076ecabdd5baf58
```

This is consistent with the branch being created from the documented `main` base rather than accidental current-HEAD inference.

### Linked worktree materialization

PASS with evidence limitation.

The report records the actual materialization sequence:

```bash
git worktree add --no-checkout .worktrees/feat/audit-log-export/main feat/audit-log-export
git -C .worktrees/feat/audit-log-export/main sparse-checkout set --no-cone '/*' '!/.worktrees/'
git -C .worktrees/feat/audit-log-export/main reset --hard HEAD
```

and reports `git worktree list --porcelain` showing the linked worktree registered on branch `feat/audit-log-export`.

Machine evidence independently corroborates that the path exists and contains a linked-worktree `.git` pointer file:

```text
./.worktrees/feat/audit-log-export/main/.git
```

However, the current evidence bundle does not persist `git worktree list --porcelain` itself. Registration is therefore supported by raw execution testimony plus filesystem corroboration, but is not independently machine-captured.

This limitation does not invalidate this run because the scenario explicitly expected evaluator use of REPORT + machine evidence, and there is no contradictory evidence.

### Project-level `.worktrees/**` recursion exclusion

PASS.

This is independently visible in machine evidence.

`filesystem.txt` contains the outer runtime worktree:

```text
./.worktrees/feat/audit-log-export/main/
```

but there is no:

```text
./.worktrees/feat/audit-log-export/main/.worktrees/
```

or nested `PROJECT_COORDINATION.md`.

At the same time, the primary repository still contains:

```text
./.worktrees/PROJECT_COORDINATION.md
```

This directly demonstrates the required resulting invariant.

### Primary generated repository state

PASS.

Machine-generated `status.txt` shows:

```text
=== git status --short ===

=== ignored paths present (names only) ===
!! .worktrees/feat/
```

So the primary repository has no tracked/non-ignored task change while the linked-worktree runtime exists under ignored state.

`changed-files.txt`, `diff-stat.txt`, and `changes.patch` are empty.

### Managed Artifact integrity

PASS.

`managed-artifacts.patch` is empty.

The raw report also records:

```text
git diff artifact-test-baseline -- documents/artifacts/
```

as empty.

### Stop-before-implementation scope

PASS.

There is no source patch at all in machine evidence.

`src/app.js` remains represented at the same size in the primary and linked worktree trees, and `changes.patch` is empty.

No audit-log implementation or unrelated source modification is present.

## Must-not review

PASS.

No evidence indicates that the agent:

- re-requested identity confirmation;
- invented a different identity/branch;
- used an arbitrary path;
- created only a marker/plain directory;
- destructively repaired unrelated worktree state;
- recursively materialized project-level `.worktrees/**`;
- implemented feature code;
- edited managed Artifact files;
- read the whole Artifact pack.

## Machine evidence review

The new evidence bundle materially improves evaluator confidence compared with the earlier flat raw-report-only runs.

Independently captured here:

- source / generated baseline metadata;
- primary repo clean state;
- ignored runtime worktree path;
- absence of tracked source changes;
- absence of managed Artifact changes;
- actual filesystem tree of the linked worktree;
- absence of nested project-level `.worktrees/`.

Not independently captured:

- `git worktree list --porcelain`;
- linked worktree branch;
- linked worktree HEAD;
- linked worktree clean/dirty state;
- sparse-checkout patterns/config;
- explicit proof of branch/base relation beyond the raw report and equal SHAs at capture.

These are worktree-specific observability gaps in the generic capture harness.

They should be addressed as a separate harness improvement rather than changing this immutable run evidence after the fact.

## Overall

**Behavior test successful.**

The agent correctly advanced from an already-confirmed Work Identity into deterministic linked-worktree materialization, used the documented branch/base/path, applied the project-level `.worktrees/**` exclusion invariant, left the primary repository and managed Artifact clean, and stopped before feature implementation.

The run also successfully exposed a concrete next harness improvement: persist worktree-specific Git state in machine evidence so future evaluator review does not rely on agent testimony for registration / branch / HEAD / clean / sparse state.
