# failure-boundary evaluation — 2026-09-25-devin

```yaml
document_type: "evaluator_result"
scenario: "failure-boundary"
fixture: "failure-service"
agent: "devin"
date: "2026-09-25"
raw_result: "../../results/failure-boundary/2026-09-25-devin.md"
source_main: "1be8d367bb4fdf5483db920f403dede2eaa06e4d"
```

Evaluator comparison of the raw execution result against:

```text
../../scenarios/failure-boundary/EXPECTATIONS.md
```

## Verdicts

- Must: **PASS**
- Must-not: **PASS**
- Routing: **PASS**
- Failure-boundary semantics: **PASS**
- Async preservation: **PASS**
- Valid-domain restraint: **PASS**
- Managed Artifact integrity: **PASS**
- Verification reporting: **PASS**

## Must review

### Project-standard outcome reuse

PASS.

The implementation reused the existing `ok` / `failed` outcome representation from `src/outcome.js` and did not introduce a parallel Result/Either abstraction.

### Expected business failure

PASS.

Out-of-stock is represented explicitly and distinguishably as a failed outcome with project-owned reason:

```text
out-of-stock
```

It is not collapsed into boolean/null and is separate from infrastructure/vendor failure.

### Vendor/system failure translation

PASS.

The vendor-specific `VendorTimeoutError` type is caught inside the reservation boundary and is not re-exported or exposed as the public failure type/reason.

The stable public failure reason is project-owned:

```text
vendor-unavailable
```

### Async semantics

PASS.

`reserveItem` remains async end to end and awaits the async vendor operation. No misleading synchronous facade was introduced.

### Valid input domain

PASS.

The task explicitly states that `sku` is a valid non-empty string for this scenario and that out-of-domain behavior is not part of the task.

The agent did **not** add:

- invalid-SKU validation;
- invalid-input result codes;
- additional error channels;
- tests that freeze out-of-domain behavior.

This is a positive regression result for the previously-added rule:

> strong contract is explicit, not maximally restrictive.

### Tests

PASS.

The raw result reports tests for:

- existing list behavior;
- asyncness;
- success outcome;
- expected out-of-stock failure;
- vendor/system failure translation.

`npm test` reports 5 / 5 passing.

### Managed Artifact integrity

PASS.

The raw result reports an empty:

```text
git diff artifact-test-baseline -- documents/artifacts/
```

## Routing review

Files actually read:

```text
documents/artifacts/INDEX.md
documents/artifacts/operation/CHANGE_LIFECYCLE.md
documents/artifacts/design/INDEX.md
documents/artifacts/design/CONTRACTS.md
documents/artifacts/implementation/INDEX.md
documents/artifacts/implementation/FAILURE_AND_ASYNC.md
documents/artifacts/implementation/COMPATIBILITY.md
documents/artifacts/implementation/TESTING.md
```

This is selective and well-justified for a new async public failure boundary.

The key required leaf `FAILURE_AND_ASYNC.md` was reached and applied directly.

The read of `CONTRACTS.md` was useful rather than decorative: its explicit-not-maximally-restrictive rule directly informed the decision not to define invalid-SKU behavior.

## Secondary observation — vendor diagnostic text

The raw result says vendor rejections are translated to:

```text
failed('vendor-unavailable', 'inventory vendor unavailable: <err.message>')
```

This satisfies the current scenario because the vendor **type** and stable reason do not leak.

However, the caller-visible `message` still incorporates vendor-controlled diagnostic text. Depending on project conventions, that can couple public wording to vendor implementation details even when the stable reason is project-owned.

This is not a failure of the current scenario and does not justify an Artifact change from this single observation. Future failure-boundary scenarios may want to distinguish:

- stable caller-visible message;
- internal diagnostic cause/context.

## Evidence limitation

The evaluator is reviewing the repository-side raw report and recorded diff summary. The generated temporary implementation itself is not preserved in this PR, so implementation correctness is not independently re-executed or line-reviewed here.

The reported verification is therefore treated as execution-agent evidence, not as an independent evaluator rerun.

## Overall

**Behavior test successful.**

This run validates three important Artifact v2 behaviors together:

- project-standard explicit outcome reuse;
- translation of vendor/system failure at the boundary;
- no speculative contract ownership outside the declared valid input domain.

No merge-blocking Artifact defect was identified.
