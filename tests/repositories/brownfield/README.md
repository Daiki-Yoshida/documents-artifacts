# Brownfield Invoice Sample

This is an intentionally small brownfield codebase.

The current public function is used by callers not represented in this fixture. Preserve its callable shape unless the task explicitly requires a contract change.
