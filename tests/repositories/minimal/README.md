# Minimal Route Library

Small JavaScript library for grid-route experiments.

There are intentionally few project-local architecture rules. Keep the implementation understandable and avoid adding infrastructure unrelated to the requested behavior.
