# Task

Add a public capability to list users whose IDs start with a supplied prefix.

Requirements:

- Empty prefix is valid and returns all users.
- Preserve existing `findUserById` behavior.
- Add tests.
- Follow this project's local instructions and existing structure.

Before changing code, use the installed reusable guidance starting from:

```text
documents/artifacts/INDEX.md
```

Use the reusable artifacts as generic guidance, but respect project-local specialization.

Do not edit `documents/artifacts/`.

In the final report, include the exact artifact files you actually read and the verification actually run.
