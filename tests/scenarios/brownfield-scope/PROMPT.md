# Task

Fix the currency rounding bug in `finalPrice`.

Expected observable behavior:

- `finalPrice` returns a JavaScript number rounded to two decimal currency precision.
- Existing public callable shape must remain compatible.
- Existing unrelated behavior must remain intact.
- Add or adjust tests if needed and run the relevant verification.

This is a focused bug fix. Do not perform unrelated cleanup merely because you notice old style or design problems.

Before changing code, use the installed reusable guidance starting from:

```text
documents/artifacts/INDEX.md
```

Read only the artifact files needed for this task.

Do not edit `documents/artifacts/`.

In the final report, include the exact artifact files you actually read and the verification actually run.
