# Task

Add a public `findPath(grid, start, goal)` capability to this repository.

Requirements:

- Movement is orthogonal only.
- `#` cells are blocked.
- The result for a reachable goal must provide the sequence of coordinates from start to goal, including both endpoints.
- Callers must be able to distinguish an unreachable goal from an invalid input.
- Keep the solution appropriate for this small project. Do not build features that are not required.
- Add meaningful tests and run the relevant verification available in the repository.

Before changing code, use the installed reusable guidance starting from:

```text
documents/artifacts/INDEX.md
```

Read only the artifact files you judge necessary for this task; do not preload the entire artifact directory.

Do not edit `documents/artifacts/`.

In your final report, include:

1. what you changed;
2. verification actually run and its result;
3. any relevant contract/design decisions;
4. the exact `documents/artifacts/...md` files you actually read.
