# documentation-routing execution result

## Run metadata

- Date: 2026-09-25
- Agent: Devin (SWE-2 Max)
- Scenario: documentation-routing
- Fixture: documented-project
- Result repository: `/tmp/documents-artifacts-agent-tests-1000/documentation-routing/repo`
- Source repository commit: `2b62b70373c33e56e1f315e32021282e49b636f6` (main at run time; matches Issue #51 baseline)
- Run baseline tag: `artifact-test-baseline` = `3ea3ec292981c013f796047cd4a7e59c24cd5a81`

## Summary

Recorded the requested HTTP retry policy as durable project knowledge in the existing owning document `documents/project/HTTP_CLIENT.md`, discovered via `AGENTS.md` → `documents/INDEX.md`. Exactly one file changed (+8 lines, new `## Retries` section). No new document, no INDEX edit, no README/AGENTS policy duplication, no restructuring.

## Project-local documentation entry/routing discovered

- `AGENTS.md`: "Read `documents/INDEX.md` first for documentation ownership and routing. Keep this file a small entry point; do not duplicate detailed policy here."
- `README.md`: durable knowledge lives under `documents/`, routed through `documents/INDEX.md`.
- `documents/INDEX.md`: routing table — "HTTP client policy (timeouts, retries, headers) → `project/HTTP_CLIENT.md`"; "Deployment and runtime operations → `project/OPERATIONS.md`"; explicit rule "Add a row only when a new owning document is created."

## Documentation ownership decision

- Retry policy concerns HTTP client behavior → the existing owner `documents/project/HTTP_CLIENT.md` already covers "timeouts, retries, headers" per the INDEX row. Updating it was a `DOC_L0_content` change (existing-file content edit).
- No new owning document was needed → `documents/INDEX.md` correctly left unchanged per its own rule.
- `README.md` / `AGENTS.md` remain entry points only; no policy copied into them.

## Documentation files changed

- `documents/project/HTTP_CLIENT.md` (+8/-0): added `## Retries` section between `## Timeout` and `## Headers`.

## Content/policy recorded

```text
- GET requests may be retried at most twice when the response is 502 or 503,
  using exponential backoff between attempts.
- POST requests are never retried automatically.
- The per-attempt timeout above still applies to every attempt; retries do
  not extend it.
```

## Existing values intentionally preserved

- Request timeout kept at the stated **8 seconds per attempt** — referenced ("the per-attempt timeout above") rather than restated, to avoid a second copy of the same durable value drifting.
- All existing sections (`Timeout`, `Headers`, `Base URLs`) untouched; existing doc style (heading + bullet list) matched.

## Artifact routing

Files actually read (in the generated repository):

- `documents/artifacts/INDEX.md` — entry router; identified the "Documentation" row.
- `documents/artifacts/documentation/INDEX.md` — documentation router; selected the two relevant leaves.
- `documents/artifacts/documentation/PRINCIPLES_AND_ROUTING.md` — INDEX-as-routing-hub, entry-file/README non-duplication, one primary concern per file.
- `documents/artifacts/documentation/WORKFLOW_AND_MAINTENANCE.md` — update the owning document + routing in the same change; DOC level classification (this change = `DOC_L0_content`); managed-artifact non-edit rule.

No other artifact files were read. (`operation/CHANGE_LIFECYCLE.md` was skipped deliberately: this was a documentation-only change with no code contract, and the documentation router already provided the workflow/level guidance needed.)

## Verification / inspection commands

Executed in the generated repository:

- `grep -n 'HTTP' documents/INDEX.md` — INDEX row "HTTP client policy (timeouts, retries, headers) → `project/HTTP_CLIENT.md`" still resolves the new content.
- `grep -rn 'retr' documents/ --exclude-dir=artifacts` — retry content exists only in the owning document; no duplicates in `documents/INDEX.md`, `OPERATIONS.md`, `README.md`, or `AGENTS.md`.
- `git status --short` — `M documents/project/HTTP_CLIENT.md` only.
- `git diff --stat artifact-test-baseline` — `1 file changed, 8 insertions(+)`.
- `git diff artifact-test-baseline` — verified the diff is purely the additive `## Retries` section.
- `git diff artifact-test-baseline -- documents/artifacts/` — empty.

Harness check before the run (source repository): `bash tests/test-agent-harness.sh` — PASS.

No test/build commands exist in this fixture; verification was inspection-based as shown above.

## Final Git state

```text
$ git status --short
 M documents/project/HTTP_CLIENT.md

$ git diff --stat artifact-test-baseline
 documents/project/HTTP_CLIENT.md | 8 ++++++++
 1 file changed, 8 insertions(+)
```

Baseline tag: `artifact-test-baseline` = `3ea3ec292981c013f796047cd4a7e59c24cd5a81`.

## Managed Artifact integrity

`documents/artifacts/` unchanged — diff against baseline is empty.

## Scope / intentionally not changed

- `documents/INDEX.md` — unchanged per its own rule (no new owning document created).
- `README.md`, `AGENTS.md` — left as entry points; no detailed policy duplicated.
- `documents/project/OPERATIONS.md` — unrelated document untouched.
- No new documentation files, no restructuring, no cleanup, no content rewrites of existing sections.
- `documents/artifacts/` unchanged.

## Information not independently preservable

None — the full change is a tracked diff visible via `git diff artifact-test-baseline`.

## Agent self-assessment

- **Unnecessary reads**: none identified. Four artifact files total, each selected by a router row that matched the task. `documentation/FORMAT_AND_GIT.md` was not needed — no new file/format conventions were introduced beyond matching existing Markdown style.
- **Missing/ambiguous guidance**: none blocking.
- **Routing uncertainty**: none. The project-local INDEX had an explicit row for exactly this concern ("timeouts, retries, headers"), which made ownership unambiguous.
- **Useful guidance**: "update the owning document and routing references in the same change" plus the DOC level scale made the minimal-change decision concrete; "do not make entry files duplicate authorities" confirmed not touching README/AGENTS.
- **Tension between project-local conventions and reusable guidance**: none — the project-local INDEX convention and the reusable INDEX-hub principle are the same model.
