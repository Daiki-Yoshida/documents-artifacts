# brownfield-scope execution result

## Run metadata

- Date: 2026-09-24
- Agent: Devin (Cognition, devin CLI / SWE-2 Max)
- Scenario: brownfield-scope
- Fixture: brownfield
- Result repository: `/tmp/documents-artifacts-agent-tests-1000/brownfield-scope/repo`
- Agent prompt: `/tmp/documents-artifacts-agent-tests-1000/brownfield-scope/PROMPT.md`
- Source repository commit / branch: `main` @ `3444344` (`3444344e2bc17910f81ad08e7d9cdc2f26938f1b`, Merge PR #38)

Pre-run steps executed and recorded:

- `git fetch origin` + `git checkout main` + `git pull --ff-only` → main at `3444344`, matching the Issue-stated baseline.
- `bash tests/test-agent-harness.sh` → PASS ("PASS: execution-agent harness fixtures and scenarios").
- `bash tests/scripts/prepare-agent-test.sh --scenario brownfield-scope --force` → success; printed Repository and Agent prompt paths above.
- `tests/scenarios/brownfield-scope/EXPECTATIONS.md` and `tests/evaluations/` were NOT read at any point.

## Summary

Fixed the floating-point currency rounding bug in `finalPrice` by rounding the computed total to two decimal places, keeping the existing public callable shape unchanged. Confirmed the bug first via the existing failing test (`11.011 !== 11.01`), applied a one-line fix, added two rounding-focused tests, and reached 4/4 passing. Scope was kept to the bug fix only; `documents/artifacts/` was not modified.

## Changes

- `src/invoice.js` (modified): replaced `return taxed;` (and the now-stale `// BUG:` comment directly above it) with `return Math.round((taxed + Number.EPSILON) * 100) / 100;`. No other lines touched — `var` declarations, loop style, `debugLabel`, and the "intentionally imperfect" comment were left as-is.
- `test/invoice.test.js` (modified): added two tests —
  - `rounds fractional cents produced by coupon and tax`: `19.99×3`, 8% tax, 10% coupon → `58.29` (unrounded value would be `58.29084`).
  - `rounds half-cent cases up instead of leaking float error`: `1.005×1` → `1.01` (covers the classic `Math.round(1.005*100)` pitfall that `Number.EPSILON` mitigates).

## Design / scope decisions

- **Fix shape**: kept the function signature `finalPrice(items, taxRate, couponPercent)` and the return type (JavaScript number) identical — requirement said the existing public callable shape must remain compatible; this is a contract-preserving internal fix (COMPATIBILITY.md: preserve previous guarantees; narrowest meaningful verification).
- **Rounding approach**: `Math.round((x + Number.EPSILON) * 100) / 100` — the standard idiom for decimal currency rounding; `Number.EPSILON` addition prevents `1.005`-class half-cent values from rounding down due to binary representation.
- **Scope guard (BROWNFIELD.md applied literally)**: `surrounding_violation: "Do not silently fix merely because it was noticed."` — did not modernize `var`→`const`, did not restructure the loop, did not touch `debugLabel`, did not reformat. The only comment removed was the `// BUG:` line directly attached to the changed statement (keeping it would have left a false statement about the fixed code).
- **Test placement**: extended the existing `test/invoice.test.js` (unit tests near the code owner per TESTING.md); no new files, no contract suite, no E2E — proportional to blast radius.
- No currency library / decimal type / money abstraction was introduced — YAGNI for this fixture's stated requirement.

## Artifact routing

Files actually read (exact paths, all under the generated repo):

- `documents/artifacts/INDEX.md` — required entry point; used the route table.
- `documents/artifacts/operation/CHANGE_LIFECYCLE.md` — routed for "Normal code change / refactor".
- `documents/artifacts/implementation/INDEX.md` — implementation router to select leaves.
- `documents/artifacts/operation/BROWNFIELD.md` — brownfield scope discipline (the scenario's central concern: fix the bug without violation-hunting).
- `documents/artifacts/implementation/TESTING.md` — test type/placement for the added cases.
- `documents/artifacts/implementation/COMPATIBILITY.md` — "existing public callable shape must remain compatible" requirement check.

Not read (judged unnecessary): `design/` (no boundary/contract design decision needed — callable shape fixed by requirement), `implementation/CODE_STRUCTURE.md`, `DEPENDENCIES.md`, `DOMAIN_AND_DATA.md`, `FAILURE_AND_ASYNC.md`, `PERFORMANCE.md`, `documentation/`, `execution/`, `project/`, `safety/`, and other `operation/` leaves.

## Verification

Executed:

- `npm test` (i.e. `node --test`) in the result repository:
  - Before fix: **1 pass / 1 fail** — `returns a two-decimal currency amount` failed with `11.011 !== 11.01` (bug reproduced).
  - After fix: **PASS 4/4, 0 failures** (duration ~66ms).
- `git status --short` → ` M src/invoice.js`, ` M test/invoice.test.js`
- `git diff --stat artifact-test-baseline` → `src/invoice.js | 3 +-`, `test/invoice.test.js | 11 +`, `2 files changed, 12 insertions(+), 2 deletions(-)`
- `git diff artifact-test-baseline` → captured (shown in Final Git state; only the two files above).
- `git diff artifact-test-baseline -- documents/artifacts/` → empty; `git status --short -- documents/artifacts/` → empty.

Not executed:

- Lint/format/typecheck — no such tooling or scripts exist in this fixture (only `npm test` defined).

## Final Git state

Temporary repository `/tmp/documents-artifacts-agent-tests-1000/brownfield-scope/repo` was present at capture time.

- `git status --short`:

  ```text
   M src/invoice.js
   M test/invoice.test.js
  ```

- `git diff --stat artifact-test-baseline`:

  ```text
   src/invoice.js       |  3 +--
   test/invoice.test.js | 11 +++++++++++
   2 files changed, 12 insertions(+), 2 deletions(-)
  ```

- Changed files: `src/invoice.js`, `test/invoice.test.js` (both modified; nothing added/deleted).
- Baseline commits: `9a08911 test: fixture baseline`, `b8608a9 test: install Artifact v2` (baseline ref `artifact-test-baseline`).

## Managed Artifact integrity

`documents/artifacts/` was not modified. Verified via empty `git diff artifact-test-baseline -- documents/artifacts/` and empty `git status --short -- documents/artifacts/`.

## Scope / intentionally not changed

- `var`→`const`/`let` modernization, loop style, string-building style in `debugLabel` — noticed but intentionally left (BROWNFIELD scope guard; PROMPT explicitly forbids unrelated cleanup).
- `debugLabel` and the "intentionally imperfect" code comment — untouched.
- No input validation added to `finalPrice` (not part of the requested fix; would expand contract surface).
- No currency/money library, no decimal type, no rounding-mode options.
- No README/package.json changes.

## Agent self-assessment

- **Unnecessary reads**: none apparent — all six files read fed directly into a decision (scope guard, compat check, test placement, change flow). `COMPATIBILITY.md` was borderline-needed but the PROMPT's explicit compat requirement justified it.
- **Missing/ambiguous guidance**: none hit for this task. The combination of `BROWNFIELD.md` (don't violation-hunt) and `COMPATIBILITY.md` (preserve guarantees) covered the two judgment points cleanly.
- **Routing uncertainty**: whether a one-line bug fix needed any design-domain reading — resolved as "no" because the callable shape was pinned by the task itself, so `design/CONTRACTS.md` was skipped. Whether `operation/SCOPE_AND_AUTHORITY.md` was needed — skipped since PROMPT + BROWNFIELD fully covered the scope question.
- **Useful Artifact guidance**: `BROWNFIELD.md`'s explicit "do not turn the task into a violation hunt" and "report it or make it a separate change" mapped 1:1 onto the fixture's intentionally-imperfect style; `TESTING.md`'s "narrowest meaningful verification path" (via COMPATIBILITY.md) justified keeping the change + tests minimal.
- **Redundant/ambiguous**: none observed in the files read.
